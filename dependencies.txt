# Python dependencies for building this wiki, pinned to exact versions so the
# build is reproducible — it will resolve identically even years from now.
# Install with: pip install -r dependencies.txt

# Direct dependencies
Flask==3.1.3
Frozen-Flask==1.0.2
Markdown==3.10.2

# Transitive dependencies (pinned for reproducibility; installed automatically
# by the packages above — listed here to lock their exact versions too)
blinker==1.9.0
click==8.4.1
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
Werkzeug==3.1.8
